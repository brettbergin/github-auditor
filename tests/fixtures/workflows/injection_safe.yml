name: Issue triage
on:
  issues:
    types: [opened]

permissions:
  contents: read

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Log title
        env:
          TITLE: ${{ github.event.issue.title }}
        run: |
          echo "New issue: $TITLE"
